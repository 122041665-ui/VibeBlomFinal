[variables]
PHP_VERSION = "8.4"

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
```

3. Guarda con **`Cmd + S`**

---

### Subir el cambio a GitHub Desktop

1. Abre **GitHub Desktop**
2. Verás el archivo modificado `VibeBloom/nixpacks.toml`
3. En **"Summary"** escribe:
```
Fix: set PHP 8.4 in nixpacks config